// Templates des prompts du flux EXPERT — SOURCE DE SEED et REPLI du code.
//
// Historiquement codés en dur dans lib/ai/expert.ts, donc invisibles dans l'admin
// (constat Alexis 2026-07-16 : « il s'appelle comment le prompt expert ? je le vois
// pas »). La DB reste la source de vérité (règle prompts Foyer) : le code résout
// `expert_swap` / `expert_iterate` / `render_hd` via resolvePrompt — éditables et
// versionnés dans l'admin comme les autres — et ne retombe sur ces constantes que
// si la ligne DB manque (seed : scripts/seed-expert-prompts.ts). Toute évolution
// durable se fait EN DB ; ces constantes ne servent qu'à re-seeder et à survivre
// à une DB incomplète.

/** Swap NB2 : incruster les produits catalogue dans le rendu. \{room}, \{mapping}. */
export const EXPERT_SWAP_TEMPLATE = `This is a beautifully styled photo of a {{room}}. YOUR TASK — MANDATORY: replace EACH listed piece of furniture with its real catalog product — but ONLY pieces actually VISIBLE in this photo: if a listed piece does not exist in the photo, SKIP it and add NOTHING for it (never insert a product into an empty spot). A listed piece VISIBLE but left UNCHANGED is a FAILURE; a listed piece replaced by a LOOKALIKE instead of the product's EXACT appearance (shape, colour, materials, details) from its reference image is a FAILURE. IGNORE the reference backgrounds. Place each product at the SAME position and orientation as the piece it replaces, at its REAL-WORLD size — respect the product's true nature and scale (a side table stays a small side table ~40-50 cm, never enlarged into a coffee or dining table; a pouf stays pouf-sized) — and ALWAYS at the product's TRUE proportions and shape from its reference image: NEVER stretch, widen, squash, enlarge or distort a product to fill the old piece's footprint (if the old piece was bigger, leave breathing room instead). When several identical pieces of the same type exist (e.g. dining chairs or bar stools), replace EVERY ONE of them with that same product, keep the same count AND the same natural arrangement: chairs stay tucked at their table, seats FACING the table — never scattered or turned away from it; remove the old pieces: {{mapping}}. Keep EVERYTHING ELSE strictly identical to this photo — do NOT change, re-tint, restyle, move OR REMOVE anything other than the furniture listed above. In particular, KEEP every other furniture piece exactly where it is AND exactly as it looks — a repainted or customized piece keeps its EXACT paint colour and finish from this photo, pixel-faithful — even next to a replaced one (e.g. if you replace the bar stools, KEEP the bar/high table they surround; if you replace dining chairs, KEEP the dining table). Also keep unchanged — EXCEPT any piece explicitly listed above, which MUST be replaced: all wall art and frames, mirrors, lamps and light fixtures, plants, vases, cushions, books, tableware and small decor, the curtains, the wall colors and finishes, the ceiling, the window, the floor, and the entire styling, lighting and camera framing. Preserve the exact exposure and white balance. STRICT RULES — violating any of these ruins the result: add NOTHING that is not in this photo or in the product list above (no extra furniture, lamp, plant or decor); NEVER add, duplicate or move a ceiling or wall light fixture — the ONE allowed change is swapping a LISTED pendant or wall sconce for its product AT THE SAME electrical point (same count of fixtures, same ceiling/wall point, never a second one, never relocated). A LISTED floor or table lamp is swapped in place, standing exactly where the old one stood, at its real-world height; every MIRROR shows a plausible reflection of THIS very room only — never an object that does not exist in the room, never a duplicated fixture in the reflection; rooms and spaces visible through open doors or wall openings stay EXACTLY as in this photo (do not furnish or restyle them); each replaced piece touches the floor with natural contact shadows — no floating objects, no object intersecting another. Photorealistic.`;

/** Itération expert (sol/peinture/objet ciblé). \{userRequest}, \{cible}. */
export const EXPERT_ITERATE_TEMPLATE = `Apply ONLY the following change to this room photo: {{userRequest}}. {{cible}}Keep EVERYTHING ELSE exactly as it is — all furniture and its exact positions, all decor, the layout, the windows, doors, ceiling, lighting, and the SAME camera angle and framing. Only change what the request explicitly asks (e.g. the floor or the wall paint). Preserve the exact perspective and a photorealistic look with natural lighting and contact shadows. Preserve the input image's sharpness, texture and grain: do NOT smooth, repaint or re-render areas you were not asked to change — copy them through untouched. STRICT RULES: add NOTHING new to the scene — no extra chair, table, seat or any other object, even if the room looks like it could use one; never duplicate an existing piece of furniture; never add, duplicate or move a light fixture; mirrors reflect THIS room only (never an object absent from the room); spaces seen through doors or openings stay exactly as they are.`;

/** Tirage HD 4K : détail/netteté uniquement, contenu figé. Aucune variable. */
export const RENDER_HD_TEMPLATE = `Upscale this interior photo to a high-resolution, print-quality image. Change NOTHING about its content: the exact same furniture, the exact same objects, at the exact same positions and orientations, the same colours, the same wall and floor finishes, the same lighting and shadows, the same camera angle, perspective and framing. Add nothing, remove nothing, move nothing, restyle nothing. Your ONLY job is to restore detail and sharpness: recover fine texture (fabric weave, wood grain, rattan, plant leaves, wall paint), crisp edges and clean lines, as if the photo had been shot at high resolution from the start. Keep it photorealistic and natural.`;
